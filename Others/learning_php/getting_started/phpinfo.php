<?php
	phpinfo();
?>

<?php

	
	// phpinfo(INFO_GENERAL); 		// value 1 The configuration line, php.ini location, build date, Web Server, System, ect...  
	// phpinfo(INFO_CREDITS); 		//  value 2 PHP 4 Credits. can also use... phpcredits().  
	// phpinfo(INFO_CONFIGURATION); //  4 Current Local and Master values for PHP directives. See also ini_get().  
	// phpinfo(INFO_MODULES); 		//  8 Loaded modules and their respective settings. See also get_loaded_extensions().  
	// phpinfo(INFO_ENVIRONMENT); 	//  value 16 Environment Variable information, like: $_ENV.  
	// phpinfo(INFO_VARIABLES); 	//  value 32 Shows all predefined variables from Environment, GET, POST, Cookie, Server (EGPCS).  
	// phpinfo(INFO_LICENSE); 		//  value 64 PHP License information. 
	// phpinfo(INFO_ALL); 			//  value -1 Shows all of the above and is the default value



?>

